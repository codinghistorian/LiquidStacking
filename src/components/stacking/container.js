function Container() {  
      return (
          <div>
                {/* input box */}
                {/* wallet connect btn || stack/unstack btn */}
                {/* You will receive / Exchange rate info */}
          </div>
                    
      );
  }

export default Container;
